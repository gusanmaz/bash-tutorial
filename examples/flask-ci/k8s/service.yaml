apiVersion: v1
kind: Service
metadata:
  name: flask-ci
  labels:
    app: flask-ci
spec:
  type: ClusterIP
  selector:
    app: flask-ci
  ports:
    - name: http
      port: 80
      targetPort: 5000
